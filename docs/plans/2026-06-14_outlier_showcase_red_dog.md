# Plan — Outlier Showcase: the off-substrate method + Red Dog worked example

> **Status**: **COMPLETE** (Phases 1–2 ✅, 2026-06-14) — the Red Dog report drafted (`../extra/reddog_bottom_up_report_2026-06.md`, **pending owner review/iteration**) + the reusable outlier playbook backfilled (`../method/outlier_playbook.md`). Kept in `plans/` (referenced); archives to `done/` at supersession. Promoted from `../discussion/2026-06-14_outlier_method_red_dog.md` (seeded by a 5-angle public-source research sweep). The **reverse-engineering / off-scope lane**: produce the Red Dog bottom-up report (the team's early-next-week deadline) **and** capture the reusable outlier method it proves. **Research-grounded** (public sources + the model + our resources) — **NOT** the public MCP, **NOT** model-gpr (owner-confirmed).
>
> **Why a lightweight plan**: this is a small, deadline-driven, deliberately off-core effort — two deliverables, not a multi-wave build. It earns a plan (discipline + the reusable-method capture), not the full phase ladder.

## 1 · What this is — the dual deliverable

```text
ONE effort → TWO outputs:
  (a) THE WORKED PROOF (deadline)   the Red Dog bottom-up report for the team — proves the method on a hard outlier
  (b) THE DURABLE ASSET             the reusable OUTLIER METHOD (the §6 playbook), so the next off-scope outlier is cheap
THE META (owner's point): the AD-HOC PATTERN = the lab's existing discussion→plan→build loop + the outlier additions
  (research-grounding · honesty label · gaps-as-findings). Capture now, prove on Red Dog, FORMALIZE into a standalone
  "ad-hoc playbook" only after a 2nd outlier uses it. Don't over-build the meta.
```

## 2 · Settled decisions (baked from the discussion §8)

| # | Decision | Resolution |
|---|---|---|
| D1 | **Deliverable** | the reusable outlier **method** + Red Dog as the **worked proof** — **not** a second Red Dog deck |
| D2 | **Lead angle** | lead the Red Dog read with the **energy-logistics chokepoint** (one ~100-day ice window gates both diesel-in and concentrate-out — the deck missed it); permafrost/water as the 2nd pillar |
| D3 | **Pace** | **report-fast** off the completed research + **backfill** the reusable method |
| D4 | **Grounding** | `external` / public research + the model + our resources; **NOT** MCP / model-gpr; **honesty-labeled** to the team as research-grounded |
| D5 | **Discipline** | the gate is unchanged — block $ EAL / PML / return-periods / plant-level forecast / single-cause; **gaps are findings**, never guesses; every claim sourced + dated + confidence + fact-vs-projection |
| — | *Open (resolve during build)* | the new resource's home/name/tag (`external`-grounded outlier class); class-vs-Red-Dog-specifics weight; how hard to lean on the Norilsk/Hjort generalization |

## 3 · The build (lean)

### Phase 1 — the Red Dog bottom-up report (the worked proof · the deadline)  ·  **status: ✅ DONE (drafted 2026-06-14, for owner review)**

**✅ Landed**: `../extra/reddog_bottom_up_report_2026-06.md` — a team-ready bottom-up read that **leads with the energy-logistics chokepoint**, then permafrost→water-balance, the fading-asset decision frame, the Norilsk-class precedent, and the **gaps-as-findings**. Every claim sourced + dated + confidence/projection-labelled; gate applied; **honestly labelled research-grounded** (no MCP / model-gpr); corrects the deck's stale points (permit extended to ~2036 · merger announced Sept-not-Dec · "$2,182M" = zinc-segment-not-company · production declining). **For owner review + iteration before any team/external use** (P4).

```text
GOAL        a team-ready bottom-up climate-risk read on Red Dog, drafted from the research sweep — the worked
            proof of the method. Honest, current, energy-angled, gap-disciplined (what the shared deck is not).
DELIVERABLES
  · the report: lead with the ENERGY-LOGISTICS CHOKEPOINT; then permafrost→water-balance crisis (the 10-yr ADEC
    permit to ~2036; Aug-2012 storm > dam design flood; the $279M pipeline); then the financial/ownership frame
    (fading asset ~2032 · NANA royalty 40→50% · Anglo merger pending · ~$558M closure + perpetual treatment);
    then the GAPS as findings (days-of-fuel-autonomy · dam factor-of-safety · fuel-farm foundation = the Norilsk gap)
  · every material claim → named source + date + confidence + fact|projection; gate applied; research-grounded label on
ACCEPTANCE  a report that beats the vanilla-LLM-+-web A/B on DISCIPLINE + CURRENCY + the energy framing (not data),
            corrects the deck's stale points, names its own gaps, and is honestly labeled research-grounded.
```

### Phase 2 — backfill the reusable outlier method (the durable asset)  ·  **status: ✅ DONE (2026-06-14)**

**✅ Landed**: `../method/outlier_playbook.md` — the reverse-engineering / off-substrate method as a lean method doc (the mode · the `external` grounding stance · the 8-step playbook · the Arctic-industrial class + frameworks/Norilsk · scope guards · Red Dog as the worked example). **Decision resolved (the §8 open item)**: a **method doc, NOT a packaged resource** — it's a cross-cutting `external`-grounded *mode*, and the grain rule doesn't cover a multi-hazard off-substrate asset; formalize/generalize only after a 2nd outlier.

```text
GOAL        lift the §6 playbook into a durable artifact so the next off-scope outlier reuses it.
DELIVERABLES
  · the OUTLIER METHOD as an `external`-grounded resource/playbook (resolve its home/name/tag — the open item);
    the class definition (remote off-grid Arctic/cold-region industrial) + the hazard stack + the citable frameworks
    (IPCC AR6 WG2 CCP6 · Hjort 2018 "adaptation is the only lever" · AMAP/SWIPA) + the Norilsk/ice-road precedents
  · tag it the AD-HOC / off-scope EXCEPTION lane; note "formalize into a standalone ad-hoc playbook after a 2nd outlier"
ACCEPTANCE  a reusable method a future session can load to reverse-engineer a new outlier, with Red Dog as its example.
```

## 4 · Scope guards (from the discussion §7)

```text
1. TAGGED EXCEPTION — the off-scope / research-grounded lane, clearly labeled; NOT a pivot of the substrate-grounded core.
2. HONESTY — research-grounded, not MCP/model-gpr; never presented as substrate-grounded product.
3. DON'T RE-DO THE DECK — additive (current · energy-angle · generalizable · gap-honest) or don't ship.
4. DON'T OVER-QUANTIFY — no $ EAL/PML/return-periods/single-cause; gaps are findings, not guesses.
5. REUSABLE METHOD = the anti-drift guard (one-off reports would be the drift); the meta stays lean until proven twice.
```

---

**See also**: `../discussion/2026-06-14_outlier_method_red_dog.md` (the discussion this resolves, with the full sourced evidence) · `README.md` (plan lifecycle) · `../use_cases.md` (the bottom-up/top-down buckets this refines) · `../method/data_map.md` (the `external` input-job) · `../method/workflow_recipes.md` (the recipe shape the playbook follows) · `../extra/reddog_lt_risk_presentation.pptx` (the existing deck this must NOT duplicate).
