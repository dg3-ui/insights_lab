# Archive — Historical-Context Raw Research

> **Status**: archive. Raw source-of-record for the distilled `historical_context.md` layer in `resources/`. Not part of the canon, not loaded at session time, never grounding.

These are the **raw deep-research outputs** that the per-package `resources/<domain>/<slug>/historical_context.md` files were distilled from. Kept here (outside `resources/`) so the full citation basis survives the distillation, per the "save the raw source, not just the output" discipline (`CLAUDE.md`).

| File | Distilled into |
|---|---|
| `extreme-cold-historical-context.md` / `.json` | `resources/hazard/extreme_cold_winter_storm/historical_context.md` (Parallel.ai deep-research run; `run_id` in the `.json`) |

**Re-verify on use.** A raw research dump is a set of *hypotheses* until each cited URL + number is checked against its source. The distilled package files are the ones that carry verified citations.
