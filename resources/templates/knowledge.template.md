# Knowledge — <Title> (cited mechanism library)

> **Status**: draft <YYYY-MM-DD>. The stable "why" — cited domain reasoning. Foundational basics come from `.learning/` (the shared vault); **live state** comes from the named external feed (pulled fresh, dated). Cite everything; this never substitutes for substrate grounding. Worked reference: `../weather_and_climate/el_nino_enso/knowledge.md`.

## 1 · The mechanism

`<driver → physical mechanism → asset outcome — the chain the resource reasons over>`

## 2 · Robustness & attribution

`<how strong is the signal? rate it. For climate/weather: the non-stationarity note — a historical baseline is an odds-shift on a shifting baseline, not a stationary law.>`

## 3 · State / measurement basis

`<the index / feed / event class that defines the current state; how to read it (e.g. ONI for ENSO, SPC reports for hail, NHC track for hurricane)>`

## 4 · Region / asset-class crosswalk

`<how the mechanism varies by geography AND by asset class — this is where the (driver × mechanism) splits live (e.g. hail×tracker ≠ hail×fixed-tilt)>`

## 5 · Citations

`<sources — peer-reviewed / agency / vault notes, with dates>`

---

**See also**: `data_requirements.md` (where to pull the live state) · `resource.yml` (the structured contract) · `.learning/` (the foundational vault).
