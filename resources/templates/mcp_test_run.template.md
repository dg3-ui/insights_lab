# Test Run 001 — <Title>

> **Status**: <YYYY-MM-DD>. The manual MCP/Claude test record (`../../docs/process/test_protocol.md`). Save the **full transcript** as raw source (P3), then resolve it into this record. Worked reference: `../weather_and_climate/el_nino_enso/test_runs/`.

## Question tested
`<one narrow question — one region · one asset class>`

## What the model retrieved (the real call sequence)
```text
<tools called, in order, with the results that grounded the draft — including the external state pull + its date>
```

## Accepted claims
`<claims that passed the gate, each with its source ref>`

## Rejected / downgraded claims
`<what was blocked or downgraded + why — tag the failure: method | MCP-tool | external-data | orchestration>`

## Fixes applied
`<fail → fix pairs (the process data that sharpens the next resource — P3)>`

## Gaps logged
`<→ ../../docs/status/mcp_gaps.md (gap · workaround · roadmap)>`

## Verdict
`<PASS / FAIL + what the loop taught (the human-checkpoint enrichment, not just the one-shot)>`
