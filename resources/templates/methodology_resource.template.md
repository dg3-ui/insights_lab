# <Title> — Method (human-readable)

> **Status**: draft <YYYY-MM-DD>. The human-readable companion to `resource.yml` (the structured operation) and `prompt_projection.md` (the session surface). Contract: `../../docs/method/resource_standard.md`. Worked reference: `../weather_and_climate/el_nino_enso/resource.md`.

## What it answers (and what it refuses)

```text
ANSWERS   <the exact questions — the question_grammar>
REFUSES   <exact $/forecast · pre-validation outreach · causal-where-directional>
```

## Scope

`<entity types · fuels/technologies · the one-region-one-asset-class rule · lifecycle · actors>`

## Mechanism (cite `knowledge.md`; do not re-derive)

`<driver → physical mechanism → asset outcome>` — claim type: `<directional | factual>`; robustness: `<rating>`. For climate/weather: state the **non-stationarity** caveat.

## Procedure (the real tool sequence)

```text
<the ordered steps — which MCP tools, in what order; include the VERIFY step (don't trust labels)>
```

## Allowed vs blocked claims

```text
ALLOWED   <claim types this may produce>
BLOCKED   <the moat-protecting list — exact $/forecast, national-from-regional, outreach-before-gate, causal-where-directional>
```

## Confidence + caveats

`<the ceiling logic — capped at the weakest input; the grounding_maturity ceiling; required caveats incl. non-stationarity / verify-the-label>`

## Actor relevance

`<who cares · why · which rendering — without distorting the method>`

---

**See also**: `resource.yml` (the structured contract) · `knowledge.md` (the cited "why") · `prompt_projection.md` (the session surface) · `data_requirements.md` (retrieval plan) · `../weather_and_climate/el_nino_enso/` (the worked reference).
