# Applied Insight 001 — <Title> (target output shape)

> **Status**: draft / validated <YYYY-MM-DD>. The validated output shape — **real entities / ids / `as_of` only, never placeholders once filled**. Meets the applied-insight thin contract (`../../docs/method/resource_standard.md`). Worked reference: `../weather_and_climate/el_nino_enso/examples/`.

## Applied Insight Draft

### Claim
`<condition + scoped entities + mechanism + evidence + confidence + caveat + actor relevance — one disciplined sentence>`

### Scope
`<the resolved asset set — real plant/queue ids, capacity, region>`

### Mechanism
`<the cited driver → mechanism → outcome chain>`

### Source References
```text
<each material claim: type (substrate|external) · source · entity/scope · field/fact · vintage/as_of · role (primary|support)>
```

### Confidence
`<level + why — capped at the weakest input and at grounding_maturity>`

### Caveats
`<the required caveats — incl. non-stationarity (climate) / verify-the-label (hazard) / partial-coverage-omit-don't-infer>`

### Actor Relevance
`<who · why · which rendering>`

### Review Trace
`<what was checked against blocked_claims + the gate — and the result>`

### Gaps / Next Fixes
`<tool/data gaps hit → logged to ../../docs/status/mcp_gaps.md>`
